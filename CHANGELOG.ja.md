# 変更履歴（Changelog）

*[English](./CHANGELOG.md) · 日本語*

**Copaky**（azooKey をベースにした独立プロジェクト）の主な変更点。フォーマットは
[Keep a Changelog](https://keepachangelog.com/) に準拠。**azooKey**（v3.x 系、MIT）を基盤としています。
[CREDITS.ja.md](./CREDITS.ja.md) を参照してください。

## [未リリース] — プレリリース（v0.1）

### 追加
- **オフラインのクリップボード管理**：プライバシー優先の再設計。DETECT（メタデータのみ。「○○からペースト」
  バナーなし）／CAPTURE（明示的なユーザー操作で読み取り）の分離、セキュア入力欄ガード（パスワード欄は決して
  取得しない）、項目サイズ上限（約 50 KB）、7 日での自動削除。ペーストボードのソースとクロックはテスト用に
  注入可能（`ClipboardHistoryManagerTests`、8/8 グリーン）。
- **ローカルファースト CI**：ローカル Mac でビルド＋テストをミラーする `scripts/ci-local.sh`（＋`--fast`）と
  `.githooks/pre-push` ゲート。
- **イタリア語インターフェース**：文字列カタログ（companion アプリと設定）のイタリア語訳を全面的に追加。
  レビューと用語統一パスを実施済み。
- **キーボードの機能ラベルのローカライズ**：改行キー（全状態）、空白、次候補、クリップボード／絵文字タブの
  「戻る」キーが、常に日本語ではなく UI 言語に追従するようになりました。新設の
  `KeyLabelType.localizedText(String)` により、**ラベル**（翻訳する）と**入力される文字**（決して翻訳しない）
  の区別を明示しています。同梱カスタードのラベルは `CustardKeyLabelStyle` → `KeyLabelType` の境界で翻訳します。
- **キーボード言語としてのイタリア語**（設定「イタリア語を使う」、**既定はオフ**）：イタリア語は専用タブを
  持たず、英語とラテン文字タブを共有します（レイアウトは同一、予測辞書のみ別）。実装は
  `VariableStates.latinKeyboardLanguage`。オンにすると言語キーが ja → en → it と巡回し、オフのときの挙動は
  従来どおりです。`it_IT` を追加した変換エンジンの Copaky フォーク（イタリア語の `UITextChecker`、
  イタリア語のみの予測、ラテン文字の候補）に支えられています — [CREDITS.ja.md](./CREDITS.ja.md) 参照。
- **西欧のアクセント記号**を QWERTY 長押しで入力：è é ê ë · ù ú û ü · ì í î ï · ò ó ô ö õ · à á â ä ã ·
  ñ · ç。
- **QWERTY 最上段の数字ヒント**（**既定はオフ**）：各キーの上に数字を小さく表示し、長押しで入力します。
- **同梱プリセットテーマ3種**：Copaky Light／Dark／Red。
- **設定の「基本設定」**：設定は短いリスト（入力方式、ライブ変換、数字ヒント、イタリア語、クリップボード
  履歴、音、触覚、キーの文字サイズ）から開き、トグル1つで全 13 セクションを表示します。検索は**常に**この
  分割を迂回するため、短いリストが隠している行を検索が取りこぼすことはありません。14 行に日本語に加えて
  イタリア語・英語の検索キーを追加しました。
- **Copaky 自身の News**：キーボード言語としてのイタリア語、数字ヒント、クリップボード履歴。
- **システムのペーストボタン — 試験実装・既定オフ・フルアクセス必須**：`SystemPasteControl` が Apple の
  `UIPasteControl` をラップし、コピーされたテキストは item provider として渡されるため、ペーストボードを
  **一切読み取りません**（`captureProvidedText` は既存の取り込み経路とガード・上限をすべて共有）。
  シミュレータにはシステムのペーストダイアログが存在せず、実機での検証が必要です（未検証）。
- **UI テストキャンペーン**（`MainAppUITests`、スキーム `CopakyUITests`）：オンボーディング、クリップボード
  タブ、アクセント記号、数字ヒント、ローカライズされたキーラベル、イタリア語の言語巡回をカバー。ローカルの
  フィクスチャページ用に `scripts/serve_test_page.sh` を追加。
- **ストア用スクリーンショットのハーネス**：`scripts/store_screenshots.sh`、`scripts/seed_sim_clipboard.sh`。

### 変更
- **完全オフライン化**：キーボード拡張だけでなく、アプリ全体のネットワークコードを**すべて**削除。companion
  アプリは GitHub の hotfix 辞書を取得せず、カスタードをネットワークからダウンロードもしません。カスタードは
  ローカルファイルからのみ読み込みます。
- **azooKey → Copaky のリブランド**：bundle id（`com.pettipol.copaky*`）、App Group、URL スキーム
  （`copaky://`）、表示名、`IsASCIICapable=true`、カスタードの associated-domains を削除。
- **UI テキストのリブランド**：オンボーディング、ヒント、設定、String Catalog、`InfoPlist.xcstrings`
  （表示名）のユーザー向け文字列を azooKey → Copaky に。ただし **azooKey のクレジットはすべて保持**
  （OSS ライセンスページ、上流 GitHub リンク、「azooKey をベースに」の記載）。
- **バージョン**を全ターゲットで **0.1**（`MARKETING_VERSION`）に統一（従来は 2.4.2／3.0.2 と不整合）。
- **CI コスト**：ホスト型 macOS の GitHub Actions ワークフローを `workflow_dispatch`（手動）に設定。
  Dependabot の頻度を daily → monthly に。
- **v0.1 は iPhone 専用**：アプリとキーボード拡張の `TARGETED_DEVICE_FAMILY` を iPhone に限定。iPad 対応は
  実機検証後の v0.2 へ延期。App Store は一度出荷したデバイスファミリーの削除を認めない（ITMS-90101）ため、
  最初からユニバーサルにすると後戻りできなくなります。
- **フォールバック言語を英語に**：プロジェクトの開発地域を日本語から英語へ変更。Copaky が対応していない
  システム言語では、日本語ではなく英語にフォールバックします。
- **ビジュアルアイデンティティ**：アプリ・オンボーディング・キーボードの azooKey のマークを CopakyMark に
  置換。同梱のチュートリアル画像を Copaky ブランドで再撮影し、アプリアイコンを再調整。
- **azooKey のクレジット文面を1つに統一**：4画面で3通りの書き方に分かれていたクレジットを、出自と
  非提携の両方を述べる1文に統一し、そのまま使用。継承ファイルの著作権ヘッダーはそのままです
  （MIT コードには帰属表示の義務があります）。
- **アプリ内の法務リンク**を `copaky.app/privacy.html` と `copaky.app/terms.html` に変更（両ページとも公開済み）。
- **同梱の絵文字辞書**を復旧（E17.0）し、`eb15a8d1` へ更新。
- **クリップボード履歴の保存形式**を版数化。アクセシビリティラベルを日英対応にし、スムーズ削除にオプトアウトを追加。
- **署名**：`DEVELOPMENT_TEAM` をプロジェクトファイルから gitignore 済みの `Copaky.local.xcconfig` へ移動。
- **輸出コンプライアンス**：キーボードの Info.plist に `ITSAppUsesNonExemptEncryption=false` を宣言。
- **上流のバージョンゲート**で Copaky 採番（0.x）のインストールを新規扱いに（`isCopakyEra`）。

### 削除
- **Zenzai → v2 に延期**：ニューラル変換機能は **v0.1 には含まれません**。`zenz` GGUF モデル
  （CC-BY-SA-4.0）はバンドルしなくなり（submodule を削除）、Zenzai の UI／トグル／設定一式と、Zenzai 専用の
  `copaky://` ディープリンク連鎖も削除しました。v0.1 は azooKey のクラシックな（非ニューラル）変換のみを
  搭載します。
- **テレメトリ**を構造的に削除：貢献／誤変換レポート／単語共有のサブシステムを撤去（データ収集なし、残存
  コードパスなし）。
- **リモートのカスタード共有**を削除：`custard.azookey.com` のデッドパスと `CustardShareHelper` を削除し、
  companion アプリのリモートカスタードダウンロードも撤去。
- **カスタード共有リンクの死んだコード**：完全オフライン化以降は動作していなかったため削除。

### 修正
- **App Group の fail-soft 化**：7 箇所の強制アンラップ `containerURL(…)!` を、コンテナが利用できない場合
  でもクラッシュせずに穏やかに劣化するように変更。
- 正直な UI：オフライン化のスタブが残していた偽の「共有済み／送信済み」成功表示を削除。
- `ContentView` のディープリンクのスキーム判定を、登録済みの `copaky` スキームに修正。
- 数字ヒント表示中も、Shift／Caps Lock で最上段の文字を大文字表示するように修正。
- キーボード初回起動時の通知ストームを除去（azooKey 旧絵文字マイグレーション由来）。
- スクリーンショット生成パイプラインの残存フェイルオープン経路（シード・クリーンアップ・マッピング）を
  失敗として顕在化。

### 既知の問題
- 上流由来の**既存**テスト失敗が 1 件
  （`UserDictionaryMigrationTests/test_migrate_known_single_placeholder_merges_into_date_format`、
  日付テンプレートのリテラルのクォート）— azooKey から継承。トリアージ待ちで、フルのローカルゲートでは
  スキップ。
- 言語キーに**長押しメニューがありません**：イタリア語を有効にした場合、3番目の言語へはキーの巡回でしか
  到達できません。

### 公開前に残っている作業
- 実機検証：RSS／jetsam 予算（≤ 40 MB）、システムのペーストボタン（`UIPasteControl`）のフロー、TestFlight。
- セキュリティ点検（ファジング、リポジトリ全体のペーストボード監査）。
- App Store 用アセットの残り（ペーストのフローのスクリーンショット）。
